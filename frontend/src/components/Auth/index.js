import {connect} from 'react-redux';
import Container from './container';

// Add all teh actions for
// Login
// Sign up
// Recover password
// Check username
// Check password
// Check email

export default connect()(Container);
