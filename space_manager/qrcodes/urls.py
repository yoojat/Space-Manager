from django.conf.urls import url
from . import views

urlpatterns = [
    # url(
    #     regex=r'^(?P<user_id>[0-9]+)/$',
    #     view=views.QR.as_view(),
    #     name='QR',
    # ),
    # url(
    #     regex=r'^temp/$',
    #     view=views.Temp.as_view(),
    #     name='QR',
    # ),
    # url(
    #     regex=r'^check/$',
    #     view=views.Check.as_view(),
    #     name='Check',
    # ),
]